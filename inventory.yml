---
- all:
    hosts:
      localhost
      erde
      mars
      jupiter
      saturn
      pluto
      uranus
    vars:
      home_user: andreas
      become_user: sauron
      conf_path: "/home/andreas/.config/"
      #vault_password_file = ~/.vault_pass.txt
